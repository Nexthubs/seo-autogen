"""Provider framework (SEO-AUTO-DEV-SPEC.md section 10).

Strict boundary (spec section 69):

    Search != Extract != Analyze != Write != Review != Render != Publish

Each capability has its own interface; implementations are swappable
through configuration.
"""
